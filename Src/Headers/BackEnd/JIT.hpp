#ifndef JIT_hpp

#define JIT_hpp

#include <vector>
#include <iostream>

class JIT
{
    public:
        JIT ();

        void parsing (std::vector <int>& from, std::vector <int>& to);

    private:
};

JIT::JIT ():
{
}

void JIT::parsing (std::vector <int>& from, std::vector <int>& to)
{
}

#endif /* JIT_hpp */
